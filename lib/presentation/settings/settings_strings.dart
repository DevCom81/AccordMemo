const settingsPageTitle = 'Paramètres';

const settingsDataSectionTitle = 'Données';

const settingsLocalOnlyMessage =
    'Vos fiches sont enregistrées uniquement sur cet ordinateur.';

const settingsLocationLabel = 'Emplacement des données';

const settingsBackupButton = 'Sauvegarder';

const settingsRestoreButton = 'Restaurer';

const settingsBackupHelp =
    'Crée une copie de toutes vos fiches, pianos, accords et rappels. Conservez-la dans vos Documents ou sur une clé USB.';

const settingsRestoreHelp =
    'Remplace les données actuelles par une copie précédente.';

const settingsBackupSuccess = 'Copie enregistrée.';

const settingsRestoreSuccess = 'Les données ont été restaurées.';

const settingsRestoreTitle = 'Restaurer une copie ?';

const settingsRestoreBody =
    'Les données actuellement présentes dans AccordMémo seront remplacées par celles de cette sauvegarde.';

const settingsRestoreConfirm = 'Remplacer les données';

const settingsCancel = 'Annuler';

const settingsWorking = 'Opération en cours…';

const settingsMailSectionTitle = 'Envoi des rappels';

const settingsMailAccountLabel = 'Compte Gmail';

const settingsMailConnect = 'Connecter un compte Google';

const settingsMailConnectedPrefix = 'Compte connecté : ';

const settingsMailReconnect = 'Reconnecter';

const settingsMailDisconnect = 'Déconnecter';

const settingsMailConnecting = 'Connexion Google…';

const settingsMailConnectCancelled = 'La connexion Google a été annulée.';

const settingsMailConnectFailed = 'Impossible de connecter le compte Google.';

const settingsMailNotConfigured =
    'L’envoi d’e-mail n’est pas encore configuré sur cet ordinateur.';

const settingsMailGenericError = 'Impossible de mettre à jour le compte Gmail.';

const settingsBackupError = 'Impossible d’enregistrer la copie.';

const settingsRestoreError =
    'La restauration n’a pas abouti. Vos données précédentes ont été conservées.';

const settingsBackupInvalid =
    'Ce fichier n’est pas une copie utilisable d’AccordMémo.';

const settingsBackupNewer =
    'Cette copie a été créée avec une version plus récente d’AccordMémo.';

const settingsBackupBusy = 'Une opération est déjà en cours.';
